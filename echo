echo "batyr"             Выводит текст **batyr** на экран и переводит курсор на новую строку
echo -n "batyr"          Выводит текст **batyr** **без перехода на новую строку** (`\n` не добавляется)
echo -e "batyr"          Включает обработку специальных символов (например, `\n`, `\t`, `\\`)
echo -E "batyr"          Отключает обработку специальных символов — они выводятся как обычный текст
