grep    Ищет строки, соответствующие заданному шаблону.

-i	Игнорирует регистр символов при поиске.
-r	Выполняет рекурсивный поиск по всем подкаталогам.
-n	Показывает номера строк с найденными совпадениями.
-v	Выводит строки, которые не соответствуют шаблону.
